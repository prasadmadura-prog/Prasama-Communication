import React from 'react';

/**
 * Auth component is deprecated as the app uses open access.
 * Returns null to ensure it doesn't render even if referenced.
 */
const Auth: React.FC = () => {
  return null;
};

export default Auth;